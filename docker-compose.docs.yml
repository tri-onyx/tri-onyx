services:
  docs:
    image: python:3.13-slim
    working_dir: /app
    command: >
      sh -c "pip install -q uv &&
             uv sync --group docs &&
             uv run mkdocs serve -a 0.0.0.0:8000"
    ports:
      - "127.0.0.1:8000:8000"
    volumes:
      - .:/app
