# Security Hunter Ledger

One line per entry, newest last.

- 2026-08-31 | FIXED | risk-model | Reject commit messages/trailers with embedded newlines so a SubmitPage file name can't forge Taint-Level/Sensitivity-Level provenance | lib/tri_onyx/repo_store.ex
- 2026-08-31 | DOWNGRADED | risk-model | InformationClassifier.controlled_path?/1 and Workspace.agent_host_path/2 classify/resolve purely on the reported path string — a symlink planted under an agent's own writable /workspace (e.g. via Bash, pointing at /repos/*, /github/*, or /reflection-logs) reads real external/sensitive content while being classified :low taint. Needs a filesystem-aware (symlink-resolving) containment check across every mount kind, not just a string-prefix test — bigger than a single-file fix, needs a human to design the right chokepoint. | lib/tri_onyx/information_classifier.ex, lib/tri_onyx/workspace.ex
- 2026-08-31 | DOWNGRADED | risk-model | InformationClassifier.controlled_path?/1 checks `String.starts_with?(normalized, container_root())` without a trailing separator (unlike its own "/repos/" branch, which has one baked in) — a sibling path like "/workspace-evil" would misclassify as controlled. Not currently reachable (no such mount exists in Sandbox.mount_plan/1 today) so not provable as a live finding, but the check is inconsistent with its own /repos/ branch and would silently reopen if a future mount ever shares that prefix. | lib/tri_onyx/information_classifier.ex
- 2026-08-31 | NO-FINDING | risk-model | Reviewed sanitizer.ex, taint_matrix.ex, sensitivity_matrix.ex, risk_scorer.ex, rate_limiter.ex. Sanitizer.type_matches?(nil, _type) accepts nil for any declared schema field type (probable type-confusion smell, not a boundary-crossing bypass — no sink found that trusts the type without also checking for nil). TaintMatrix/SensitivityMatrix "unknown trigger type -> :low" fallback is only reachable from internal atoms, not attacker input. RiskScorer is an advisory-only static computation at agent creation ("warns but does not block"), not a runtime gate. |
